import React from 'react'

const ReactLazyLoadingScreen = () => (
  <div className="d-flex w-full align-items-center justify-content-center" style={{ minHeight: '80vh' }}>
    <p>Loading...</p>
  </div>
)

export default ReactLazyLoadingScreen;
